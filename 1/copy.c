#include <stdio.h>

/* copy input to output; 1st version */

int main() {
  int c;

  printf("EOF is: %d\n", EOF);
  printf("getchar() != EOF 0 or 1? %d\n", getchar() != EOF);
  //c = getchar();
  //while (c != EOF) {
  //  putchar(c);
  //  c = getchar();
 // }
}
